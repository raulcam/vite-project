export const Siderbar = () => {
    // const collapsed = useNavigationStore(state => state.collapsed);
    // const handleChangeCurrent = useNavigationStore(state => state.handleChangeCurrent);
    // const navigation = useNavigationStore(state => state.navigation);
    return ( 
       <aside style={{width:100, background:"#01A3D1", height:"100vh"}}>
        <p>sidebar</p>
       </aside>
     );
}