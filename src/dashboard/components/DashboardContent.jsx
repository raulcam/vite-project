import { useEffect } from "react";
import { useDispatch, useSelector } from "react-redux";
import api from "../../api/api";

export const DashboardContent = () => {
  
  return <div>Contenido de la pagina principal aqui va la tabla CRUD</div>;
};
